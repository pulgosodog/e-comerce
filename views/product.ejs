<section class="product-detail" style="max-width:1100px;margin:24px auto;padding:20px">
  <div style="display:flex;gap:24px;align-items:flex-start;">
    <div style="flex:1;max-width:480px">
      <img src="<%= product.image_url || '/images/placeholder.png' %>" alt="<%= product.name %>" loading="lazy" width="480" height="320" style="width:100%;border-radius:8px;object-fit:cover" data-fallback="/images/placeholder.png" />
    </div>
    <div style="flex:1.2">
      <h1 style="margin:0 0 8px"><%= product.name %></h1>
      <p style="color:#666;margin:0 0 12px">Marca: <strong><%= product.brand || 'N/A' %></strong></p>
      <p style="font-size:20px;margin:8px 0">
        <% if (product.is_on_sale && product.price_sale) { %>
          <span style="color:var(--accent);font-weight:700">$<%= product.price_sale %></span>
          <span style="text-decoration:line-through;margin-left:8px;color:#999">$<%= product.price_regular %></span>
        <% } else { %>
          <span style="font-weight:700">$<%= product.price_regular %></span>
        <% } %>
      </p>
      <p style="margin:8px 0;color:<%= product.stock && product.stock > 0 ? '#0a8b3a' : '#d9534f' %>">Stock: <strong><%= product.stock %></strong></p>
      <p style="color:#333;margin-top:12px"><%= product.description %></p>

      <div style="margin-top:16px;display:flex;gap:12px;align-items:center">
        <input id="qty" type="number" value="1" min="1" max="<%= product.stock || 1 %>" style="width:80px;padding:8px;border:1px solid #e0e0e0;border-radius:8px" <%= product.stock <= 0 ? 'disabled' : '' %> />
        <% if (product.stock > 0) { %>
          <button id="add-to-cart-detail" class="btn btn-primary" data-product-id="<%= product.product_id %>">Añadir al carrito</button>
        <% } else { %>
          <button id="add-to-cart-detail" class="btn" style="background-color: #ccc; color: #666; cursor: not-allowed;" disabled data-product-id="<%= product.product_id %>">No disponible</button>
        <% } %>
      </div>
    </div>
  </div>

</section>
